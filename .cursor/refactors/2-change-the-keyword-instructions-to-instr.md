Right now, we have two keywords: instructions and rules. The instructions keyword is too long for people to keep typing. Example: `cfs instructions view`. We should use a short form such as `instr.` 

