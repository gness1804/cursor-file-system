Right now, we have two keywords: instructions and rules. The instructions keyword is too long for people to keep typing. Example: `cfs instructions view`. We should use this short form: `docs`. As in: `cfs docs view`.

